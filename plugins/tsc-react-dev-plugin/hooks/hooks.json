{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f node_modules/.bin/prettier ] && jq -r '.tool_input.file_path' | xargs -I {} npx prettier --write {} --ignore-unknown || true"
          }
        ]
      }
    ]
  }
}
